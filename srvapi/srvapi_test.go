package srvapi_test
// TODO: write tests that actually make sense instead of letting AI try
/*
import (
	"FOOdBAR/srvapi"
	"FOOdBAR/views/viewutils"

	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/labstack/echo/v4"
	"github.com/stretchr/testify/assert"
)

func TestHTML(t *testing.T) {
	e := echo.New()
	req := httptest.NewRequest(http.MethodGet, "/", nil)
	rec := httptest.NewRecorder()
	c := e.NewContext(req, rec)

	err := srvapi.HTML(c, http.StatusOK, nil)

	assert.NoError(t, err)
	assert.Equal(t, http.StatusOK, rec.Code)
	assert.Equal(t, echo.MIMETextHTML, rec.Header().Get("Content-Type"))
}

func TestTabMaximizeRenderer(t *testing.T) {
	// Set up mock context, page data and tab data
	e := echo.New()
	req := httptest.NewRequest(http.MethodGet, "/", nil)
	rec := httptest.NewRecorder()
	c := e.NewContext(req, rec)

	pageData := &viewutils.PageData{}
	tabData := &viewutils.TabData{}

	// Call TabMaximizeRenderer
	err := srvapi.TabMaximizeRenderer(c, pageData, tabData)

	// Assert expected behavior
	assert.NoError(t, err)
	assert.Equal(t, http.StatusOK, rec.Code)
}

*/
